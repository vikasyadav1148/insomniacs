/* slim menu mobile */
!(function (a, b, c, d) {
    "use strict";
    function e(b, c) {
        (this.element = b), (this.$elem = a(this.element)), (this.options = a.extend(h, c)), this.init();
    }
    var f = "ht_menu",
        g = 0,
        h = { resizeWidth: "992", initiallyVisible: !1, collapserTitle: "", animSpeed: "fast", easingEffect: "linear" };
    (e.prototype = {
        init: function () {
            var c,
                d = a(b),
                e = this.options,
                f = this.$elem,
                g = '<div class="menu-collapser">' + e.collapserTitle + '<div class="collapse-button ion-android-menu"></div>';
            f.before(g),
                (c = f.prev(".menu-collapser")),
                f.on("click", ".sub-toggle", function (b) {
                    b.preventDefault(), b.stopPropagation();
                    var c = a(this).closest("li");
                    a(this).hasClass("expanded")
                        ? (c.children(".sub-toggle").removeClass("expanded"), c.find(">ul, .mega-menu").slideUp(e.animSpeed, e.easingEffect))
                        : (c.children(".sub-toggle").addClass("expanded"), c.find(">ul, .mega-menu").slideDown(e.animSpeed, e.easingEffect));
                }),
                c.on("click", ".collapse-button", function (a) {
                    a.preventDefault(), f.slideToggle(e.animSpeed, e.easingEffect);
                }),
                this.resizeMenu(),
                d.on("resize", this.resizeMenu.bind(this)),
                d.trigger("resize");
        },
        resizeMenu: function () {
            var c = a(b),
                e = c.width(),
                f = this.options,
                h = a(this.element),
                i = a("body").find(".menu-collapser");
            b.innerWidth !== d && b.innerWidth > e && (e = b.innerWidth),
                e != g &&
                    ((g = e),
                    h.find("li").each(function () {
                        a(this).has("ul, .mega-menu").length &&
                            (a(this).addClass("has-submenu").has(".sub-toggle").length
                                ? a(this).children(".sub-toggle").html(f.expandIcon)
                                : a(this)
                                      .addClass("has-submenu")
                                      .append('<span class="sub-toggle">' + f.expandIcon + "</span>")),
                            a(this).children("ul, .mega-menu").hide().end().find(".sub-toggle").removeClass("expanded").html(f.expandIcon);
                    }),
                    f.resizeWidth >= e
                        ? (h.addClass("collapsed").find("li").has("ul, .mega-menu").off("mouseenter mouseleave"), i.show(), f.initiallyVisible || h.hide())
                        : ($("#menu-primary-menu .sub-menu, #menu-primary-menu .mega-menu").show(), h.removeClass("collapsed").show(), i.hide()));
        },
    }),
        (a.fn[f] = function (b) {
            return this.each(function () {
                a.data(this, "plugin_" + f) || a.data(this, "plugin_" + f, new e(this, b));
            });
        });
})(jQuery, window, document);
